---
title: 自定义国际化
description: 在应用中新增或覆盖 Hearth GPUI 的翻译。
order: -3
---

# 自定义国际化

Hearth GPUI 为组件提供了内置翻译。应用可以新增语言或覆盖个别翻译，而不需要复制完整的内置 locale 文件。

此功能需要 `rust-i18n` 4.2 或更高版本。

## 添加依赖

在持有 locale 文件的应用 crate 中添加 `rust-i18n`：

```toml
[dependencies]
hearth-gpui = "0.5"
rust-i18n = "4.2"
```

## 创建应用 locale 文件

在应用 crate 中创建 `locales/ui.yml`，并将组件翻译放在 `hearth_gpui` namespace 下：

```yaml
_version: 2
hearth_gpui:
  Calendar:
    week.0:
      fr: Di
    month.January:
      fr: Janvier
  DatePicker:
    placeholder:
      fr: Sélectionner une date
```

namespace 必须是 `hearth_gpui`，它对应 Rust crate 名称 `hearth-gpui`，其中连字符转换为下划线。可在 [Hearth GPUI 内置 locale 文件](https://github.com/JonZhang3/hearth-gpui/blob/main/crates/ui/locales/ui.yml)中查看可用的翻译 key。

## 注册扩展

首先在应用的 crate root 初始化 locales：

```rust
rust_i18n::i18n!("locales", fallback = "en");
```

然后在初始化 Hearth GPUI 之前注册扩展：

```rust
app.run(move |cx| {
    rust_i18n::extend!(hearth_gpui);
    hearth_gpui::init(cx);

    // 打开窗口并初始化应用的其他部分。
});
```

应用启动期间只需调用一次 `extend!`。

## 查找优先级

应用提供的翻译优先于组件的内置翻译：

```text
应用 locales（locales/ui.yml）
             │
             │ 未找到 key
             ▼
Hearth GPUI 内置 locales
```

这种 deep merge 行为意味着：

- 新语言（例如 `fr`）只需提供应用需要的 key。
- locale 和 key 都相同时，应用提供的翻译会覆盖内置值。
- 应用未提供的 key 会继续使用组件内置值。
- Hearth GPUI 后续新增的翻译无需复制，也会自动可用。

例如，只定义 `hearth_gpui.Calendar.month.January.en` 会修改一月份的英文标签，其他英文日历标签仍然来自 Hearth GPUI。

## 切换语言

使用 rust-i18n 修改当前 locale：

```rust
rust_i18n::set_locale("fr");
```

组件随后会按照上述优先级，使用当前 locale 查找翻译文本。
