interface:
  display_name: "Align shadcn Component"
  short_description: "Compare and align GPUI components with shadcn Vega"
  default_prompt: "Use $align-shadcn-component to compare a GPUI component with the pinned local shadcn implementation, produce an alignment plan, and wait for approval before implementation."
